@model CardViewModel

<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title">@Model.Title</h5>
        <p class="card-text">@Model.Quantity</p>
        <a href="#" class="btn btn-primary">@Model.Description</a>
    </div>
</div>